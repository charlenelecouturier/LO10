import { HttpURLInterceptor } from './http-urlinterceptor';

describe('HttpURLInterceptor', () => {
  it('should create an instance', () => {
    expect(new HttpURLInterceptor()).toBeTruthy();
  });
});
