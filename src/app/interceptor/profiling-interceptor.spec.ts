import { ProfilingInterceptor } from './profiling-interceptor';

describe('ProfilingInterceptor', () => {
  it('should create an instance', () => {
    expect(new ProfilingInterceptor()).toBeTruthy();
  });
});
